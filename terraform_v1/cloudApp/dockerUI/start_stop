fut-e a gép: gcloud compute instances list
ssh:
    gcloud compute ssh gpu-worker-c --zone=europe-west1-c
docker_stop:
    cd ~/stack
    sudo docker compose down
vm_stop:
    
    
vm_start:
    gcloud compute instances start gpu-worker-c --zone=europe-west1-c
ssh:
    gcloud compute ssh gpu-worker-c --zone=europe-west1-c
docker_start:
    cd ~/stack
    sudo docker compose up -d
verification:
    sudo docker compose ps
    ha jó   mediamtx   Up
            judge      Up

⚠️ Fontos sorrend

INDÍTÁS:

VM start

Docker up

Pi stream start

UI open

LEÁLLÍTÁS:

UI close

Docker down

VM stop

Pi stream stop